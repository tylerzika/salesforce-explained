- Confirmed with debug log and Apex docs: class `static` variables persist across all trigger/code-unit invocations inside one execution context (transaction), then reset at transaction boundary.
- `TriggerEventLogger.logCalls` increasing from 1..14 in one anonymous Apex run is expected behavior, not cross-transaction leakage.
- `scripts/apex/campaign-member.apex` models core Agentforce Sales (Sales Cloud) data path: Account, Contact, Campaign (2), Opportunity, CampaignMember (2), OpportunityContactRole.
- Added TAOCP/SICP easter-egg comments through triggers/classes to make transaction-state mental model memorable.
- Added CLRS loop-invariant framing for trigger logging: `logCalls` and per-object counts should be monotonic within one execution context.
- Added tests that prove: (1) monotonic counter behavior and (2) `AFTER_INSERT` is not a reliable transaction-end signal.
- Log evidence also showed platform code units (for example, DuplicateDetector) can run after trigger events, reinforcing that trigger code generally cannot assert execution-context end.
- Added role-prep guidance comments across trigger stack (Platform Engineer now, Solution Architect trajectory).
- Updated all session-critical agent files (`AGENTS.md`, `SOUL.md`, `USER.md`, `MEMORY.MD`, `IDENTITY.md`) with fresh collaboration profile notes.
- Model log for this session: GPT-5 (Codex).
